# Optional SatQuery Backend Base URL (without trailing slash).
# Leave unset for the Vercel same-origin /api proxy. Set it only when running
# the frontend against a backend directly, such as in local development.
# Example: https://satquery.onrender.com
VITE_API_BASE_URL=https://your-backend-url
