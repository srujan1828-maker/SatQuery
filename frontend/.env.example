# Optional local Vite proxy target (without trailing slash). This value is used
# by the development server only; production browser requests always use the
# same-origin Vercel /api proxy.
# Example: https://satquery.onrender.com
VITE_API_BASE_URL=https://your-backend-url
