# Backend URL used directly by the browser in development and production
# (without a trailing slash). Configure this same value in Vercel and redeploy.
# The backend must allow the Vercel frontend origin through CORS.
# Example: https://satquery.onrender.com
VITE_API_BASE_URL=https://your-backend-url
